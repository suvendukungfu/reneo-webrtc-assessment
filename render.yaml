services:
  - type: web
    name: reneo-webrtc-signaling
    env: node
    region: oregon
    buildCommand: npm --prefix server install && npm --prefix server run build
    startCommand: npm --prefix server run start
    envVars:
      - key: PORT
        value: 8080
